export MODEL_PATH=models/model.onnx
export INPUT_SIZE=640
# optionally set local fallback
export USE_AWS=false
python main.py
# or with uvicorn directly:
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
